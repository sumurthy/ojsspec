1. Use TypeScript AST to generate the intermediary JSONs.
2. @param or any other parameters doesn't take more than 1 line. Fix it using the array element:
function processLines(element, index, array) {
  ...
}
3. Modules are not supported. Need recursive way to support them.
