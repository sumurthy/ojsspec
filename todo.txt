21. sp-Client-preview.ts
4. HANDLE OBJECT TYPE OF DATA: (similar to module/object data type support)
  interface IHtmlProperties {
    imageLinkProperties: {
      [ propName: string ]: string
    }
    linkProperties: {
      [ propName: string ]: string
    }
    searchableProperties: {
      [ propName: string ]: string
    }
  }
5. Check Optional handling ? for methods, params, etc. Need it in markdown.
17. Can't have random numbers name. Maybe we can do Sha256 strings.
19. IBasicHttpClientOptions - has headers data type problems, no hyperlink for BodyInit
20. Links to variables and types
21. Handle {} inside strings.
22. Need to handle module inside module. Need to handle VAR as parameter

declare module "es6-collections" {
    var Map: MapConstructor;
    var Set: SetConstructor;
    var WeakMap: WeakMapConstructor;
    var WeakSet: WeakSetConstructor;
}
22. Ignore export = lines
	export = rsvp;
23. handle module inside module

LONGER TERM:
19. Make use of dFunction for enum and function file generation
1. Use TypeScript AST to generate the intermediary JSONs.
3. Modules are not supported. Need recursive way to support them.
6. properties/methods/functions: [{},{},{}] or {{}, {}, {}} ?
13. Change allTypes to set instead of an array
